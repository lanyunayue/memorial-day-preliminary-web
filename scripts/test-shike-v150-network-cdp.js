const CDP=process.env.SHIKE_CDP_URL||'http://127.0.0.1:9251';
const APP=process.env.SHIKE_APP_URL||'http://127.0.0.1:8765/';
const sleep=(ms)=>new Promise((resolve)=>setTimeout(resolve,ms));
function assert(value,message){if(!value)throw new Error(message);}
class Client{
  constructor(url){this.url=url;this.id=0;this.pending=new Map();}
  async open(){this.ws=new WebSocket(this.url);await new Promise((resolve,reject)=>{this.ws.onopen=resolve;this.ws.onerror=reject;});this.ws.onmessage=(event)=>{const msg=JSON.parse(event.data);if(msg.id&&this.pending.has(msg.id)){const item=this.pending.get(msg.id);this.pending.delete(msg.id);msg.error?item.reject(new Error(msg.error.message)):item.resolve(msg.result||{});}};}
  send(method,params={}){const id=++this.id;this.ws.send(JSON.stringify({id,method,params}));return new Promise((resolve,reject)=>{this.pending.set(id,{resolve,reject});setTimeout(()=>{if(this.pending.has(id)){this.pending.delete(id);reject(new Error(`${method} timeout`));}},60000);});}
  async eval(expression){const reply=await this.send('Runtime.evaluate',{expression,awaitPromise:true,returnByValue:true,userGesture:true});if(reply.exceptionDetails){const detail=reply.exceptionDetails.exception&&reply.exceptionDetails.exception.description;throw new Error(detail||reply.exceptionDetails.text);}return reply.result&&reply.result.value;}
}
async function main(){
  const targets=await fetch(`${CDP}/json`).then((response)=>response.json()),target=targets.find((item)=>item.type==='page');assert(target,'CDP page target missing');const client=new Client(target.webSocketDebuggerUrl);await client.open();await client.send('Runtime.enable');await client.send('Page.enable');await client.send('Network.enable');await client.send('Network.clearBrowserCache');await client.send('Storage.clearDataForOrigin',{origin:new URL(APP).origin,storageTypes:'all'});await client.send('Page.navigate',{url:APP});let ready=false;for(let i=0;i<50&&!ready;i++){await sleep(200);ready=await client.eval(`typeof ShikeRetrievalEngine==='object'&&typeof ShikeAgent==='object'&&typeof ShikeWatchCenter==='object'`);}assert(ready,'v1.5 runtime modules did not load');
  const result=await client.eval(`(async()=>{ShikeRetrievalCache.clear();const before=(window.records||[]).length;const providers=ShikeRetrievalEngine.providers();const local=ShikeRetrievalEngine.classify('打开日历');const knowledge=ShikeRetrievalEngine.classify('GitHub 上有哪些 PWA 开源项目？');let github=await ShikeRetrievalEngine.search('GitHub 上有哪些 PWA 开源项目？',{classification:knowledge,timeout:9000});if(!github.ok){await new Promise((resolve)=>setTimeout(resolve,800));github=await ShikeRetrievalEngine.search('GitHub 上有哪些 PWA 开源项目？',{classification:knowledge,timeout:9000});}const weatherClass=ShikeRetrievalEngine.classify('北京现在天气怎么样？');const weather=await ShikeRetrievalEngine.search('北京现在天气怎么样？',{classification:weatherClass,timeout:9000});ShikeAgent.clearContext();const agent=await ShikeAgent.handle('GitHub 上有哪些 PWA 开源项目？');let unsafeRejected=false;try{ShikeWatchCenter.validateFeedUrl('javascript:alert(1)');}catch(error){unsafeRejected=true;}const storage=ShikeWatchCenter._internal.getStorage();storage.setEnabledSources(['github']);storage.clearWatchItems();ShikeWatchCenter.addWatchKeyword('PWA');await ShikeWatchCenter.refresh({reason:'test'});const feed=ShikeWatchCenter.getFeed();const after=(window.records||[]).length;return{version:APP_VERSION,providers,local,knowledge,github:{ok:github.ok,count:github.sources.length,attempted:github.attempted,failures:github.failures,answer:github.answer,source:github.sources[0]||null,fallbacks:github.fallbackLinks},weather:{ok:weather.ok,count:weather.sources.length,source:weather.sources[0]||null},agent:{retrieval:agent.retrieval,count:(agent.sources||[]).length},unsafeRejected,feedCount:feed.length,feedLive:feed.every((item)=>item.isLive===true&&/^https?:/.test(item.url)),feedSources:[...new Set(feed.map((item)=>item.sourceId))],before,after};})()`);
  if(process.env.SHIKE_DEBUG_NETWORK==='1')console.log(JSON.stringify(result,null,2));assert(result.version==='v2.0.0-rc3','version mismatch');assert(['wikipedia','wikidata','github','stackexchange','open-meteo'].every((id)=>result.providers.includes(id)),'provider registry incomplete');assert(result.local.kind==='local','local operation classification failed');assert(result.knowledge.kind==='network'&&result.knowledge.domain==='github','knowledge classification failed');assert(result.github.ok&&result.github.count>0,'GitHub retrieval returned no public result');assert(result.github.source&&result.github.source.url&&result.github.source.title&&result.github.source.fetchedAt,'unified source schema incomplete');assert(result.github.fallbacks.length===3,'search fallback links missing');assert(result.weather.ok&&result.weather.source&&/Open-Meteo/.test(result.weather.source.source),'Open-Meteo retrieval or attribution failed');assert(result.agent.retrieval&&result.agent.count>0,'Agent did not route knowledge query to retrieval');assert(result.before===result.after,'knowledge query modified local records');assert(result.unsafeRejected,'dangerous RSS protocol accepted');assert(result.feedCount>0&&result.feedLive,'Watch Center did not cache real public items');assert(result.feedSources.every((id)=>id==='github'),'Watch Center source selection ignored');console.log(`v1.5 network CDP acceptance passed: GitHub=${result.github.count}, weather=${result.weather.count}, watch=${result.feedCount}`);client.ws.close();
}
main().catch((error)=>{console.error(error.stack||error);process.exit(1);});
