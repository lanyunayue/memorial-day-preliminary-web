# 敏感数据处理

取件码按高敏感数据处理：数据库只存 AES-GCM 密文；完整值默认遮挡，用户通过设备生物识别或锁屏凭据验证后才临时显示。应用启用 `FLAG_SECURE`，防止敏感页面进入截图和最近任务预览。

日志和 Operation Journal 不含完整取件码、原始通知、手机号或详细地址。遮挡导出固定输出 `pickupCode: "masked"`。不实现自定义密码算法，不提交密钥，删除全部数据时同时删除 Keystore 别名。

当前 Alpha 未提供完整敏感导出。
