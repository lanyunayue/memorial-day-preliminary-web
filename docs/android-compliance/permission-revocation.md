# 权限撤销

“撤销通知访问并清除捕获数据”会执行：

1. 立即停止应用内 NotificationSourceAdapter。
2. 切换到手动模式并清空来源白名单。
3. 删除快递、草稿、时间线与取件码密钥。
4. 打开 Android 通知访问设置，由用户关闭系统开关。

Android 不允许普通应用自行关闭 Notification Listener 系统授权，因此界面明确说明最后一步需要用户操作。系统权限关闭后，服务不再收到后续通知；手动输入和系统分享仍可用。
