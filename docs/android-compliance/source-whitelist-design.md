# 来源白名单设计

- 默认集合为空。
- 用户在原生“来源应用”页面逐项开启。
- 白名单以包名保存在应用私有 SharedPreferences。
- NotificationListenerService 首先检查包名和自动化模式，随后才读取通知字段。
- 撤销操作立即停止适配器、切换到手动模式并清空白名单。
- 当前预置候选仅用于方便选择，不代表默认授权或合作关系。

未知包名不会被自动加入。未来若增加已安装应用选择器，也必须保持显式逐项授权。
